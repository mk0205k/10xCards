---
import Layout from "@/layouts/Layout.astro";
import SignUpForm from "@/components/auth/SignUpForm";
import { m } from "@/paraglide/messages.js";
import { ERROR_CODES } from "@/lib/error-messages";

const rawError = Astro.url.searchParams.get("error");
const isPendingDeletion = rawError === ERROR_CODES.ACCOUNT_PENDING_DELETION;
---

<Layout title={m.auth_signup_title()}>
  <div class="bg-cosmic flex min-h-screen items-center justify-center p-4">
    <div class="w-full max-w-sm rounded-2xl border border-white/10 bg-white/10 p-8 text-white backdrop-blur-xl">
      <h1
        class="mb-6 bg-gradient-to-r from-blue-200 to-purple-200 bg-clip-text text-center text-2xl font-bold text-transparent"
      >
        {m.auth_signup_title()}
      </h1>
      <SignUpForm serverError={rawError} client:load />
      {
        isPendingDeletion && (
          <p class="mt-3 text-center text-sm text-blue-100/70">
            <a href="/auth/signin" class="text-purple-300 hover:underline">
              {m.auth_signup_pending_deletion_link()}
            </a>
          </p>
        )
      }
      <p class="mt-4 text-center text-sm text-blue-100/60">
        {m.auth_signup_have_account()}{" "}
        <a href="/auth/signin" class="text-purple-300 hover:underline">{m.auth_signup_link_signin()}</a>
      </p>
    </div>
  </div>
</Layout>
